zuul:
  ignored-services: "*"
  prefix:  /api
  routes:
    userservice: /user/**
    gameservice: /game/**
  sensitiveHeaders: Cookie,Set-Cookie
  debug.request: true


hystrix:
  command:
    default:
      execution:
        timeout:
          enabled: false
#        isolation:
#          thread:
#            timeoutInMilliseconds:
#              30000
